var link_list = [];

function create_a_link(hyperlink) {

	link_list[].push(hyperlink);
}

function add_links_to_page(list) {

	//Adds all of the links in link_list to the webpage in the
	//form of html elements.
}

function retrieve_links_from_file() {

	//Loop through the file (links.txt) to get how many lines
	//there are in it (excluding the ones that start with '#').
	//That value will be used for i in the for loop below.

	for(var i = 0; i < 100; i++) {

		//Go into the file and
	}
}

retrieve_links_from_file();

add_links_to_page();